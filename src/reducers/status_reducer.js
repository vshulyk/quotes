// import { CHECK_ANSWER } from '../actions/check_answer_action.js';

export default function getStatus(state = 0, action) {

	// switch( action.type ) {
	// 	case STATUS_:
	// 		return action.payload;
	// };

	return state; 
}